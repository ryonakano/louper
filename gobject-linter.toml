# See https://github.com/bilelmoussaoui/gobject-linter/blob/main/CONFIG.md for configuration

[rules.type_style]
# Do not enforce consistent use of GLib or C standard type aliases
# because we use both of them propertly
level = "ignore"
